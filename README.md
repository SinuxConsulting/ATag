# ATag Website

Frontend for ATag.

## Local development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```

## Notes
- Brand assets (logo + icons) live in `public/` and `src/assets/`.
- Backend/Auth/Forms wiring can be connected via your preferred API layer.
